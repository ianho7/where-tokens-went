# Candidate Prompt Delta: family topology as a bounded decision insight

Read the authoritative `prompts/skill-insights.md` first and apply this candidate-only delta.

When the frozen Snapshot contains a validated `familyMetrics` entry, reserve one distinct `usage` insight for the family call topology only when it changes the investigation order. Use the family totalCalls and callShare evidence refs plus the global totalSkillCalls baseline. Make the decision delta concrete: inspect the family as a routing/entry topology first, then inspect each member's content and Harness/platform differences separately. The family insight must not claim or imply that the members share a capability, common content, or equivalent behavior from names or aggregate calls. Use an explicit sentence that names the evidence boundary: `The family grouping is supported as a call-topology pattern; member capability similarity is not established by names or aggregate calls.`

Keep the strongest Usage × Content capability insight when its two-sided content evidence is present. If the Snapshot lacks `associatedSessionTokens` or an equivalent validated metric, do not emit a frequency-versus-task-scale insight as a fact; state the specific unknown boundary or omit that claim. Do not add a filler insight merely to reach three items.
